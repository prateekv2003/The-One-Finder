"use client";
import React from "react";
import Forms from "../../../components/DetailForm/Forms";
type Props = {};

const Page = (props: Props) => {
    return (
        <div>
            <Forms />
        </div>
    );
};

export default Page;
